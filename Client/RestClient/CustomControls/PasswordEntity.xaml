<UserControl x:Class="RestClient.CustomControls.PasswordEntity"
             x:Name="Ctrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24*"/>
            <RowDefinition Height="59*"/>
            <RowDefinition Height="24*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="164*"/>
            <ColumnDefinition Width="29*"/>
        </Grid.ColumnDefinitions>

        <Rectangle 
            Stroke="Black" 
            StrokeDashCap="Round" 
            StrokeEndLineCap="Round" 
            RadiusX="15" 
            RadiusY="15" 
            StrokeThickness="3" Grid.ColumnSpan="3" Grid.RowSpan="3" />
        <Label Grid.Row="1" x:Name="PassEntity" Content="" FontSize="36" Width="145" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Grid.Column="1" Padding="0" Margin="0,-8,0,0" />
    </Grid>
</UserControl>